version: '3.8'

services:
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=bomberos_db
      - POSTGRES_USER=bomberos_user
      - POSTGRES_PASSWORD=bomberos_pass
    ports:
      - "5432:5432"

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DB_NAME=bomberos_db
      - DB_USER=bomberos_user
      - DB_PASSWORD=bomberos_pass
      - DB_HOST=db
      - DB_PORT=5432
    depends_on:
      - db
    tty: true
    stdin_open: true

volumes:
  postgres_data: